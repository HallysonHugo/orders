// ignore_for_file: constant_identifier_names

import 'package:flutter/material.dart';

class DecorationUtils{
  static const DEFAULT_PADDING = EdgeInsets.only(left: 15, right: 15);
  static const DEFAULT_LEFT_PADDING = EdgeInsets.only(left: 15);
  static const DEFAULT_VSEPARATOR = SizedBox(height: 12);
  static const DEFAULT_HSEPARATOR = SizedBox(width: 7);
  static const double DEFAULT_CARD_ELEVATION = 0.0;
}